spring:
  application:
    name: pawEver
  profiles:
    active: local
  task:
    scheduling:
      pool:
        size: 5
logging:
  config: classpath:logback-spring.xml
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
openapi:
  service-key: ${OPENAPI_SERVICE_KEY}
kakao:
  api:
    key: ${KAKAO_REST_API_KEY}


